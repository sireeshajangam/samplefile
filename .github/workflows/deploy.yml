name: Deployment
on: push
jobs:
 test:
  runs-on: ubuntu-latest
  steps:
   - name: Get code
     uses: actions/checkout@v3
   - name: Install Python
     uses: actions/setup-python@v4
     with:
      version: 3.10
   - name: Run Test
     run: python main.py
 deploy:
   needs: test
   runs-on: ubuntu-latest
   steps:
    - name: Deploy Code
      run: echo "Deploying....."